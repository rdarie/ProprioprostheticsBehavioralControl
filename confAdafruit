begin
  remote = *
  button = KEY_PLAYPAUSE
  prog = training
  config = play
end

begin
  remote = *
  button = KEY_STOP
  prog = training
  config = quit
end

begin
  remote = *
  button = KEY_BACK
  prog = training
  config = back
end

begin
  remote = *
  button = KEY_VOLUMEUP
  prog = training
  config = volumeup
end

begin
  remote = *
  button = KEY_VOLUMEDOWN
  prog = training
  config = volumedown
end

begin
  remote = *
  button = KEY_KP0
  prog = training
  config = 0
end

begin
  remote = *
  button = KEY_KP1
  prog = training
  config = 1
end

begin
  remote = *
  button = KEY_KP2
  prog = training
  config = 2
end

begin
  remote = *
  button = KEY_KP3
  prog = training
  config = 3
end

begin
  remote = *
  button = KEY_ENTER
  prog = training
  config = enter
end

begin
  remote = *
  button = KEY_UP
  prog = training
  config = up
end

begin
  remote = *
  button = KEY_DOWN
  prog = training
  config = down
end

begin
  remote = *
  button = KEY_LEFT
  prog = training
  config = left
end

begin
  remote = *
  button = KEY_RIGHT
  prog = training
  config = right
end

begin
  remote = *
  button = KEY_SETUP
  prog = training
  config = right
end
